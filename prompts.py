import json
import os
import logging

from config import PROMPTS_FILE

logger = logging.getLogger(__name__)


class PromptManager:
    """
    Manages loading and accessing prompts for the summarizer.

    It first loads default prompts defined in the class, and then overrides them
    with any custom prompts defined in an external `prompts.json` file.

    Prompts can be accessed as attributes, e.g., `prompts.CHANNEL_SUMMARY_PROMPT`.
    """

    def __init__(self):
        self._defaults = {
            "COVERAGE_AND_MATCH_PROMPT": """
Сравни ТЕМУ нового сообщения с дайджестами.
- Та же тема → номер дайджеста
- Новая тема или существенные новые детали → "НЕТ"
- Разные модели/версии = разные темы
Ответь только номером или "НЕТ".
""",
            "NLP_RELEVANCE_PROMPT": """
Релевантен ли текст для NLP/ML/AI/Python/лингвистического дайджеста?

ПРИНИМАЙ: статьи, модели, библиотеки, бенчмарки, датасеты, конференции, Kaggle/хакатоны с AI, вакансии/увольнения в AI, BigTech/AI-ассистенты, LLM, промпт-инжиниринг, LoRA, диффузия, GPU, робототехника, AI-стартапы/M&A, AI-мемы, ШАД (не реклама).

ОТКЛОНЯЙ: курсы, платное обучение, реклама GigaChat/YandexGPT, коммерция, hiring days, ссылки на ботов без контекста.

Ответь только 'да' или 'нет'.
""",
            "UPDATE_SUMMARY_PROMPT": """Вставь ссылку {new_link} в подходящее место саммари.
Скопируй саммари, вставь ссылку рядом с релевантным абзацем. Остальное не меняй.
Сохрани HTML-форматирование: <b>, <a href>, теги и сущности.
Нет подходящего места — добавь "Доп. источники: {new_link}" в конец.
Только обновлённое саммари. Без вводных слов и заключений.""",
            "CHANNEL_SUMMARY_PROMPT": """
Дайджест Telegram-сообщений по NLP. Русский язык, термины — на языке оригинала.

Структура: группируй по темам, указывай [1], [2]; при совпадении темы — все номера.
Объединяй близкие подтемы под один заголовок.

Правила:
- Без введения, заключения и мета-комментариев («в этом дайджесте…», «итого» и т.п.)
- Пиши максимально кратко, без вводных слов и оборотов
- Одно предложение на факт, без пояснений
- Нейтральный стиль: факты, не мнения и не дословные рекомендации авторов
- Не повторяй одну и ту же информацию в разных разделах
- Конкретные результаты и факты, не общие описания
- Каждый абзац — одна мысль. Без воды и пояснений очевидного
- Без нумерованных и маркированных списков — только сплошной текст с абзацами
- Без подзаголовков внутри раздела — один заголовок на тему
- Заголовки: короткие, с 1–3 несинонимичных эмодзи. Пример: '<b>🧠💧 Переходы между AI-компаниями</b>'
- Только HTML: <b>текст</b>, не Markdown
- Только номера источников [1], [2], без собственных ссылок
- Максимум {max_summary_length} символов
""",
            "GROUP_SUMMARY_PROMPT": """
Дайджест сообщений из Telegram-группы по NLP. Русский язык, термины — на языке оригинала.

Структура: группируй по темам, указывай [1], [2]; при совпадении темы — все номера.
Объединяй близкие подтемы под один заголовок.

Правила:
- Без введения, заключения и мета-комментариев («в этом дайджесте…», «итого» и т.п.)
- Пиши максимально кратко, без вводных слов и оборотов
- Одно предложение на факт, без пояснений
- Нейтральный стиль: факты, не мнения и не дословные рекомендации участников
- Не повторяй одну и ту же информацию в разных разделах
- Конкретные результаты и факты, не общие описания
- Каждый абзац — одна мысль. Без воды и пояснений очевидного
- Без нумерованных и маркированных списков — только сплошной текст с абзацами
- Без подзаголовков внутри раздела — один заголовок на тему
- Не добавляй вопросы пользователей, на которые нет ответа
- Заголовки: короткие, с 1–3 несинонимичных эмодзи. Пример: '<b>🧠💧 Переходы между AI-компаниями</b>'
- Только HTML: <b>текст</b>, не Markdown
- Только номера источников [1], [2], без собственных ссылок
- Максимум {max_summary_length} символов
""",
        }

        self._prompts = self._defaults.copy()

        prompts_file = PROMPTS_FILE
        file_path = os.path.join(os.path.dirname(__file__), prompts_file)
        if os.path.exists(file_path):
            try:
                with open(file_path, "r", encoding="utf-8") as f:
                    custom_prompts = json.load(f)
                    self._prompts.update(custom_prompts)
            except (json.JSONDecodeError, IOError) as e:
                logger.warning("Could not load or parse %s: %s", prompts_file, e)

    def __getattr__(self, name):
        """Allows accessing prompts as attributes."""
        if name in self._prompts:
            return self._prompts[name]
        raise AttributeError(f"'PromptManager' object has no attribute '{name}'")


# Singleton instance to be used across the application
prompts = PromptManager()
